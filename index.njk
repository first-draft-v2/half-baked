---
layout: base.njk
---
{% set cardColors = [
  "oklch(96.6% 0.005 106.5)"
] %}
<div>
  {% for post in collections.posts | sortByLastDate %}
    {% if post.content %}
      <article class="post" >
        <p class="post-eyebrow">{{ post | extractDate | date("MMM d, yyyy") }}</p>
        <h2 class="post-title"><a href="{{ post.url | url }}">{{ post.data.title }}</a></h2>
        {% if post.data.image %}
          <img class="post-image" src="{{ post.data.image | url }}" alt="{{ post.data.imageAlt or '' }}">
        {% endif %}
        <div class="post-body">{{ post.templateContent | safe }}</div>
        {% if post.data.tags %}
          <div class="post-tags">
            {% for tag in post.data.tags %}
              {% if tag != "post" %}<a href="{{ ('/tags/' + tag + '/') | url }}">#{{ tag }}</a>{% endif %}
            {% endfor %}
          </div>
        {% endif %}
      </article>
    {% endif %}
  {% endfor %}
</div>
